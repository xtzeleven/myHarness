# MySQL 只读连接（用于 mcp-server-mysql / schema-analyst agent）
# 数据库账号必须只有 SELECT 权限。强制 readonly。
MYSQL_HOST=
MYSQL_PORT=3306
MYSQL_RO_USER=
MYSQL_RO_PASS=
MYSQL_DB=

# 可选：gitnexus 索引数据库位置（已用过则可跳过）
# GITNEXUS_INDEX_PATH=
